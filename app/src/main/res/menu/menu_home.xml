<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@id/home_menu_action_search"
        android:icon="@drawable/ic_search"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView"
        />

    <item android:icon="@drawable/ic_sort"
        android:title="Sort"
        app:showAsAction="always">

        <menu>

            <item android:id="@id/home_menu_action_sort_name"
                android:title="Sort by Name"/>

            <item android:id="@id/home_menu_action_sort_date"
                android:title="Sort by date"/>

        </menu>

    </item>

    <item android:id="@id/home_menu_action_show_favorites"
        android:title="Show favorites"
        android:checkable="true"
        app:showAsAction="never"/>

</menu>